<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Crypto Price Checker</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Crypto Price Checker</h1>
  <p>Enter a cryptocurrency name (e.g., bitcoin, ethereum, litecoin)</p>
  <form action="/get-price" method="POST">
    <input type="text" name="coin" placeholder="Enter coin name" required>
    <button type="submit">Get Price</button>
  </form>

  <% if (price) { %>
    <div class="result">
      <h2><%= coin %>: $<%= price %></h2>
    </div>
  <% } %>
  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } %>
</body>
</html>